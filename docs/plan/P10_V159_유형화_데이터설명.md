# [세션 ② · #27 merge 직후 · D2보다 먼저] PR-J — V159 데이터 유형화(표출 6유형 × 구조 4형태) + 데이터 설명 영역(요소명·간략 정의·상세 설명·활용 방법·활용 사례) + 용역사 인계물

권장 설정: Opus 5 (1M) · effort max(유형 템플릿 설계) → high(적재·화면) · 플랜 모드. worktree `..\nigt-wt-d0`에서 `git checkout main && git pull`(#27 merge 후) → `feat/v159-typology-spec`. **병렬 세션** — 편집 금지: `public/data/vietnam/v2/*`(P7), `src/components/map/*`·`src/map/*`·`RealMapExplorerPage.tsx`·`DetailLocationMapV148.tsx`(P3). 프레임워크 xlsx는 `_source/spec/db_status_framework_v5.38_260922.xlsx`에 두고(미커밋) 생성 JSON만 커밋한다.

---

너는 이 저장소의 데이터 제품 리드다. `CLAUDE.md`, `docs/plan/V159_데이터유형화_명세.md`(v2, 6×4 모델·배정표; 이 프롬프트와 함께 `docs/plan/`에 복사), `docs/VISUALIZATION_CONTRACT_V153.md`·`src/data/visualization/publicVisualizationContractV153.json`(D1의 152행 계약, `archetype` 필드), `src/components/data/public/PublicDataAnalysisRouterV126.tsx`, `src/pages/DataExplorerPage.tsx`(카드), `src/pages/CountryDataElementPage.tsx`(히어로), `src/data/visualization/facilityCardV153.ts`, `koreanTermsV153.json`, 그리고 `_source/spec/…xlsx`의 시트 `▲데이터_명세서_260907`(열: 요소명_플랫폼·간략 정의·상세 설명·활용 방법·정의(국문)·출처기관·참고문헌 링크·참고문헌(APA)·확인일자·금년도 최종 결정)·`▲활용사례_260922`(366행)·`▲Framework_260907`(지역 구분·기술 분류 플래그)·`▲국가별_충족매트릭스_260907`을 읽고 계획을 먼저 제시하라.

## 배경(사실)
- 용역사가 "현재 탑재 데이터를 유형화하지 않으면 데이터 구조화 작업 때문에 과업기한(2026-10-30) 내 플랫폼 완성 불가"라고 통보. 따라서 152개를 개별 설계하지 않고 **표출 유형 6개(유저 질문 기준: ① 국가 수준·추세 ② 지역·입지 ③ 기술별 비교 ④ 시설·기관·인프라 위치 ⑤ 사업·재원 ⑥ 제도·규제·리스크, + ⓪ 상태 안내) × 데이터 구조 4형태(S1 국가×연도 관측값 · S2 지역×연도 관측값 · S3 위치 개체 · S4 비공간 개체)**로 구현한다. 교차 점유는 13칸(거의 대각선). 유형 밖 전용 처리는 6건(A-023·B-046/047·D-004·A-013·B-008·E-006)으로 제한. 근거·배정표는 명세 v2.
- 명세서에 152개 전부 `요소명_플랫폼`·`간략 정의`(52~90자)·`상세 설명`(156~338자)·`활용 방법`(187~297자)·정의·출처·참고문헌이 작성돼 있고, 활용 사례 366건(143개 요소, 요소당 1~3건)이 5차 납품 데이터와 대조 검증돼 있다. 화면 문구는 이 파일을 **그대로 읽는다**(수기 작성·요약·윤문 금지).
- 첨부 「플랫폼 구조 변경(안) 1/3~3/3」 채택: 카드=출처 윗줄+원데이터명+간략 정의 / 상세=제목 아래 '데이터 설명' 영역(상세 설명·활용 방법·활용 사례 접힘) / 사례 카드 6항목.

## 목표
1. `src/data/spec/datasetTypologyV159.json`(152행: displayType ①~⑥/⓪ · structure S1~S4 · flags · status · variant)와 **표출 템플릿 6개 + 구조 어댑터 4개**로 상세 라우팅을 유형 기반으로 전환(계약 `archetype` → `displayType`/`structure` 정합, 전용 컴포넌트는 6건만 유지·나머지는 템플릿으로 흡수하되 화면 회귀 0). 각 템플릿에 유형별 '판단 포인트' 고정 블록(자동 계산: 순위·5년 방향·상위 3 지역·유망 기술 3·건수·총액·최신 개정 등) 포함. 38대 기후기술 필터는 전 유형 공통 UI.
2. 데이터 설명 영역·카드 명칭 규칙 구현(전 요소).
3. 용역사 인계물: `docs/DATA_TYPOLOGY_V159.md` + `output/v159/datasetTypologyV159.xlsx` + 유형별 대표 화면 스크린샷 6장 + 구조별 예시 CSV 4개. **착수 당일** 구조 4형태 스키마 초안(`docs/DATA_TYPOLOGY_V159_SCHEMA.md`)을 먼저 커밋해 용역사가 구조화를 시작하게 한다.

## 구현
### A. 명세 적재(스크립트, 수기 0)
- `scripts/v159/import-dataset-spec-v159.mjs`: xlsx → `datasetSpecV159.json`(elementId·platformName·sourceLabel·baseName·shortDefinition·shortDefinitionCard·description·usage·definitionKo·sourceOrg·refLink·refApa·checkedAt·decision) + `useCasesV159.json`(elementId·caseNo·purpose·purposeEn·logic·dataUsed[{indicatorId,label}]·storyline·users[]·caution·cautionDisplay·verified) + `datasetTypologyV159.json`(명세 v2 §4 배정표 그대로; displayType·structure·플래그·상태·변형).
- 명칭 분리 규칙: `platformName`을 `sourceOrg`(국문/영문 별칭표) 접두로 분리 → `sourceLabel` + `baseName`. 분리 실패는 규칙표 `sourceLabelRulesV159.json`(예: "EU JRC EDGAR 외", "World Bank WDI", "WRI Climate Watch")로 보정, 최종 152/152 분리 + 검수표 출력.
- `shortDefinitionCard`: 문두 "기관명(영문)이/가 " 절 제거 규칙 → 152건 결과표를 `reports/v159/short-definition-card-review.md`에 남겨 눈검토(문법 깨지는 건 원문 유지).
- `cautionDisplay`: 유의점의 타국 언급("캄보디아·라오스는 값이 없다" 등)은 국가 레지스트리 수록국 기준으로 치환(원문은 툴팁), 치환 목록 보고.
- `dataUsed[].indicatorId`(예 `A-001_cpi_score`)를 카탈로그 지표에 매핑(매핑률 보고, 미매핑은 라벨만).
- D-001·D-002·D-004 사례 2건씩 같은 서식으로 작성(`verified: "pending"`, 배지 '검증 대기').

### B. 유형 템플릿
- 템플릿 `src/components/data/templates/{U1CountryProfile,U2Regional,U3Technology,U4Locations,U5ProjectsFinance,U6Policy,U0Status}V159.tsx` + 구조 어댑터 `src/data/structure/{S1CountryObservation,S2RegionObservation,S3LocatedEntity,S4Entity}V159.ts`(납품 시트 → 템플릿 입력 스키마 정규화; 용역사 인계 스키마와 1:1). 입력은 (카탈로그 요소, 팩 데이터, 계약 행, typology 행, 플래그). 축·단위는 계약값 → `ChartAxesV150`. 지도 자리는 기존 `DetailLocationMapV148`(P3 후 MiniMap) 그대로 꽂는다. 판단 포인트 블록 `DecisionPointsV159.tsx`(유형별 계산 규칙 문서화, 값 없으면 숨김·추정 금지).
- 라우터: typology의 `displayType`으로 템플릿, `structure`로 어댑터 선택, `dedicated: true` 6건만 전용 컴포넌트. 기존 전용 구조(A-016·D-011·A-002·E-012·D-005·C-018·C-012·B-002 등)는 템플릿 옵션으로 흡수하되 **스크린샷 대조로 회귀 0** 증명(픽셀 차이 허용치 명시).
- 계약 정합: `publicVisualizationContractV153.json`의 `archetype`을 `displayType`+`structure`로 치환·검증(불일치 목록 보고 후 계약 우선 수정).

### C. 화면
- 데이터 찾기 카드: 출처 윗줄(작게) + 원데이터명(제목) + `shortDefinitionCard`; '주 사용자'(공공기관·기업·금융기관·연구자)·'유형(①~⑥)' 필터 추가; 수치·차트·기간·버튼 불변.
- 상세: 히어로(출처 줄·원데이터명·국가 + 간략 정의 1문장) → '데이터 설명' 영역(상세 설명 / 활용 방법 / 활용 사례 N건 접힘 — 사례 카드: 활용 목적+영문 키워드 / 논리 구조 / 쓰는 데이터 칩(클릭 시 1순위 차트 계열 강조, 없으면 비활성) / 스토리라인 인용 / 주 사용자 칩 / 유의점) → 분석 차트(D1 순서). 출처 1줄에 `refApa`·`refLink`·`checkedAt` 반영. 제외·대체 요소는 ⓪ 상태 안내(결정·사유·결정일). 데이터 설명 아래에 유형별 '판단 포인트' 블록.
- 접근성: 접힘 영역 `aria-expanded`, 칩 키보드 포커스, 320px 넘침 0.

### D. 용역사 인계물
- `docs/DATA_TYPOLOGY_V159.md`(표출 6유형·구조 4형태·플래그·전용 6건·배정표·판단 포인트 규칙), `output/v159/datasetTypologyV159.xlsx`(시트: 표출유형 정의 / 구조 스키마 S1~S4 / 152 배정표 / 명칭 분리표 / 사례 통계), 유형별 대표 상세 스크린샷 6장(① A-003 · ② B-003 · ③ A-018 · ④ A-023 · ⑤ D-022 · ⑥ C-009), 구조별 입력 예시 CSV 4개, 스키마 문서 `docs/DATA_TYPOLOGY_V159_SCHEMA.md`(납품 시트 열 → 어댑터 필드 → 템플릿 입력).

## 검증
- 단위: 명칭 분리 152/152, 간략 정의 길이 50~90, 사례 스키마, 타국 문구 치환, 배정 152/152(displayType·structure 모두)·전용 6건 이하·판단 포인트 계산 규칙(값 없으면 숨김).
- `qa:detail-contract:v153 --ids <전체>` + 신규 `qa:typology:v159`(각 요소 첫 블록 = 유형 1순위, 데이터 설명 영역 4요소 존재, 판단 포인트 블록 존재(값 없으면 숨김 허용), ⓪는 차트 0) → `reports/v159/typology-qa-v159.json` 152/152. 대표 12화면 회귀 스크린샷 + 6폭 넘침 0. 전체 게이트 PR 직전 1회.

## 산출·PR
- 위 문서·JSON·xlsx·스크린샷, `reports/v159/REVIEW_V159.md`(유형별 수, 전용 6건, 회귀 결과, 매핑률, 치환 목록), 추적표 '유형' 열 추가, `CHANGELOG.md`.
- 커밋 분리: 명세 적재 / 유형 JSON·계약 정합 / 템플릿 / 화면 / 인계물·QA.
- `gh pr create --base main --title "feat(v159): 데이터 유형화 표출 6유형×구조 4형태 · 데이터 설명 영역(명세서 문구) · 판단 포인트 · 용역사 인계물"` → 로컬 게이트·Vercel Preview → 보고 후 승인 대기.

## 금지
- 명세서 문구 수기 작성·요약·윤문, 표출 유형 7개 이상·구조 5형태 이상 신설, 전용 처리 7건 이상, 판단 포인트 값 추정, 계약을 화면에 맞춰 역작성, 지도·데이터 파일 편집, 게이트 반복·CI 대기.
