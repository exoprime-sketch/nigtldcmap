# V152 진행 기록 (P3 · PR-C)

- 브랜치: `feat/v152-minimap-icons` (origin/main 325b355에서 분기, 2026-09-24)
- 계획: 공용 미니맵(확대·이동) + 데이터별 지도 아이콘. 사용자 결정 — 홈은 히어로 '주요 지역 데이터' 지도만, 엔진은 조작 의도 시(마우스 300ms·포커스·터치·클릭·버튼) 활성화, 시간 경과 자동 활성화 없음.
- 수용기준(요약): 렌더러 추출 스타일 스냅샷 차이 0 · 점 계열 15개 레이어 분류값 아이콘 누락 0 · 미니맵 조작 실측(휠·드래그·버튼·키보드·터치)·해제 확인 · 콘솔 오류 0 · 6폭 넘침 0 · analysis QA 신규 실패 0 · main.js 증가 0

## 단계
| 단계 | 내용 | 상태 |
|---|---|---|
| 0 | 기준 측정(빌드·번들·스타일 스냅샷 before, 결정성 확인) | 완료 |
| 1 | 렌더러 추출(동작 불변) — 스냅샷 94회 차이 0, qa:map:v138 42/42 동일 | 완료 |
| 2 | 아이콘 모듈·라이선스(W1 서브에이전트) — 35종·테스트 134건, 커밋 626fb69 | 완료 |
| 3 | 미니맵 엔진·상세/홈 통합·인계 — 러너 9화면×25검사·터치 2×6 통과, 콘솔 0 (커밋 993905d) | 완료 |
| 4 | 큰 지도·미니맵 아이콘·범례·팝업 적용 — 15개 레이어 등록 100%·styleimagemissing 0·범례=지도 (커밋 993905d, ③과 합침) | 완료 |
| 5 | 첫 로드 경량화·결함 수정·러너 재실행·문서 → finalize:v140 1회·e2e → PR | 진행(게이트 전) |

## 기록
- 큰 지도에는 이미 localhost 전용 QA 핸들(`__cdpMapV151`, `__nigtMapObserverV137`)이 있어 새 훅을 넣지 않음. 미니맵도 같은 규칙(`__cdpMiniMapV152`).
- 추출 전 빌드 `tmp/build-v152-before`: main.js 2,042,653 B · maplibre 청크 1,062,816 B · 지도 페이지 청크 274,024 B · 상세 지도 청크 17,010 B.
- 렌더러 추출: 페이지 9,297 → 7,893줄. `src/map/layers/*` 14개 모듈. 스냅샷 비교 `reports/v152/renderer-extraction-diff-v152.json`(PASS, 차이 0), 요약 `renderer-extraction-verification-v152.json`.
- 미니맵 러너에서 찾은 실제 결함 3건과 조치: ① MapLibre CSS(`.maplibregl-map{position:relative}`)가 엔진 컨테이너의 절대 위치를 덮어써 캔버스가 지도 상자 아래로 밀림 → 바깥 래퍼/안쪽 MapLibre 컨테이너 분리 ② 헤더 버튼까지 스크롤하는 동안 지도가 화면 밖으로 나가 해제되며 카메라를 지워 '큰 지도에서 비교'가 위치를 잃음 → 해제돼도 마지막 카메라 유지, 첫 맞춤 상태만 넘기지 않음 ③ 줌 12 한계에서 A-023 클러스터가 끝까지 안 풀림 → 미니맵만 clusterMaxZoom 11(큰 지도 13 유지) + 클러스터 클릭은 실제 확장 줌
- 출처 줄(좌상단)이 좁은 지도에서 여러 줄로 접혀 클릭을 가로챔 → pointer-events: none
- 비교 모드(MapComparisonWorkspaceV135) 아이콘 적용은 보류: 창별 색 구분이 V135 감사 계약(FINANCE_COMPARE_DISTINCT_SYMBOLS) — 창 색 링 변형·범례 개편이 함께 필요해 후속으로 기록
- 첫 로드 측정(2026-09-24): 추출 전 빌드 대비 홈·선/면 상세 gzip +19.8 KB → +5.9 KB. 원인은 정적 경로에 딸려 온 아이콘 래스터·등록 코드, 배경지도 모듈(9.6 KB raw), 공개 레이어 제목 모듈(10.7 KB raw) → 아이콘 묶음 동적 청크(`map-icon-kit-v152`, 지점 레이어만)·배경 판단은 엔진으로·제목은 컴포넌트가 원래 쓰던 `publicShortTitle`
- 활성 직후 빈 지도(레이어 소스 로드 전 전환) → 엔진이 데이터 소스 로드까지 기다린 뒤(최대 2.5초) active
- 레거시 감사 15개 개별 실행: 14 PASS, `map-interaction:v129`만 FAIL — 지도 카드 12개를 기다리는 v129 기대값(현재 42개)이라 main에서도 map-load 단계 시간 초과(게이트 밖). 이 PR로 바뀐 것은 소스 문자열 검사 `publicPopup` 1개(팝업 빌더를 `src/components/map/mapPublicPopupV129.ts`로 옮김)
- 카드 숫자 결측→0 기존 결함(C-025 262건) 수정, 점 팝업 소재지 중복 주석 제거, KR 표시 CSS 오류 수정
