# 152개 상세화면 의미 검토 · 판정 (Stage 4 최종화)

후보: `CANDIDATE_FINGERPRINT_V137.json` 기준. 증거는 `qa-verified/<run>/snapshots`,
원천 대조는 `screen-review/element-source-facts-v137.json`.

판정은 화면을 열고 원천과 나란히 읽어 내린 것이다. 방문·수집만으로 KEEP 을 만들지 않았다.
각 화면에서 확인한 것: 핵심 질문 / 첫 화면 제공 정보 / 원천에 있으나 빠진 정보 /
차트·표·KPI 의 데이터 적합성 / 선택조건과 결과의 일치 / 불필요한 내부정보·중복설명.

판정값: KEEP · FIX_ANALYSIS · FIX_COPY · FIX_LAYOUT · DATA_LIMITATION · NO_DATA
"수정함" 은 이번 세션에서 고치고 해당 화면을 다시 수집해 확인한 것이다.

---

## 기준화면 (완성도 참조)

| 요소 | 판정 | 근거 |
|---|---|---|
| A-016 1차 에너지 소비구조 | KEEP | 단위(EJ)·관측기간(1965–2025, 61개 연도)·분모("여섯 에너지원 합계를 100%")·중복 경고("공급 총계는 구성항목과 중복되므로 면적 합계에 포함하지 않습니다")를 모두 화면에서 말한다. 표시기간 프리셋과 기준연도 선택이 결과를 바꾼다. |
| D-011 개도국 내 각국별 ODA | KEEP | 불변가격 기준(USD 2024)·기간·제공기관(OECD CRS)·"총계·소계는 순위에 중복 포함하지 않습니다"를 명시. 공여자 선택이 결과를 바꾼다. |
| B-005 가뭄 위험 | FIX_ANALYSIS → 수정함 | 31,688행을 두고 "SPEI-12 전망 관측값이 없습니다"라고 표시하고 있었다. 아래 §시나리오 계열 참조. |

## 시나리오·성/시 계열 (원천이 개체 속성 열로 납품)

이 계열이 이번 검토에서 가장 큰 결함이었다. 관측 시트 대신 개체 속성 열로 값이 오면서
관측 기반 렌더러가 아무것도 찾지 못했고, 화면은 원시 레코드 키 12개를 제목으로 나열했다.

| 요소 | 판정 | 확인한 내용 |
|---|---|---|
| B-003 평균 기온·강수 | FIX_ANALYSIS → 수정함 | 8,371행. 지금은 연강수량 median 1,637mm(1901) → 1,724mm(2025), 63개 성·시 분위. 기본 표시항목이 경계면적이던 것도 함께 고침 |
| B-004 CMIP6 기후전망 | FIX_ANALYSIS → 수정함 | 33,232행 · 5개 측정항목 · 6개 시나리오. 연평균기온 median 24.23℃(1950) → 27.99℃(SSP3-7.0, 2100) |
| B-005 가뭄(SPEI12·CDD) | FIX_ANALYSIS → 수정함 | 31,688행. SPEI-12·연속건조일수 두 항목이 시나리오별로 표시됨 |
| B-006 폭염 | FIX_ANALYSIS → 수정함 | 32,608행 |
| B-007 홍수(RX1day 등) | FIX_ANALYSIS → 수정함 | 32,370행 |
| B-008 해수면 상승 | DATA_LIMITATION | 1,575행이나 지역이 PSMSL 관측소이고 성·시가 아니다. 성·시 분포로 읽을 수 없어 목록 표시를 유지 |
| B-017 물 스트레스 | DATA_LIMITATION | 2,032행 단일 시점. 연도 차원이 없어 추이가 성립하지 않음 |
| B-026·B-029·B-030·B-037·B-039·B-040·B-041·B-042 | DATA_LIMITATION | 대부분 전국 1행 + 성·시 1행 구조라 연도 축이 없다. 목록 유지가 맞고, 카드 제목은 아래 §공통 수정으로 읽을 수 있게 됨 |

## 공통 수정 (여러 화면에 걸침)

| 결함 | 영향 | 조치 |
|---|---|---|
| 원시 레코드 키가 카드 제목 (`VNM.1_1_ssp370_2096`) | 19개 화면 | 지역·시나리오·연도로 제목 구성 ("AnGiang · SSP3-7.0 · 2096년") |
| 카드마다 같은 설명 12회 반복 | 39개 화면 중 카드형 전체 | 그리드당 1회로 이동 |
| 카드 배지에 내부 레코드 종류 `entity` 노출 | 카드형 전체 | 구조 토큰은 배지에서 제외 |
| 음수 부호가 공개 문구 정규화기에서 삭제 | 값이 음수인 모든 화면 | 부호 뒤 숫자면 다듬지 않음 + 회귀 테스트 |
| `formatPublicDeltaV126` 가 단위를 항상 "점"으로 표기 | 전년대비를 쓰는 화면 | 단위를 인자로 받음 |

## 자료 없음 (원천에 값이 없음 — 결함 아님)

| 요소 | 판정 | 화면이 말하는 것 |
|---|---|---|
| C-020 · C-021 · C-023 | NO_DATA | "아직 공개된 값이 없습니다 공식 자료를 확보한 뒤 제공합니다" |
| E-011 · E-013 | NO_DATA | "입력 항목만 있고 실제 값은 아직 없습니다" |

## 개별 판정

| 요소 | 판정 | 근거 |
|---|---|---|
| A-002 거버넌스 지표(WGI) | FIX_ANALYSIS + FIX_COPY → 수정함 | CPIA 1–6점 척도를 설명하면서 WGI 데이터를 담고 있었고 아무 값도 표시하지 않았다. 6개 부문 백분위 추이·연도 선택·표시값 선택으로 교체하고 척도 설명을 원천에 맞게 고침 |
