# 152개 상세화면 의미 검토 · 판정 (Stage 4 최종화)

후보: `CANDIDATE_FINGERPRINT_V137.json` 기준. 화면 수집은
`screen-review/screen-census-v137-1440.json`, 원천 대조는
`screen-review/element-source-facts-v137.json`, 문구 점검은
`screen-review/public-copy-audit-v137.json`.

판정은 화면을 열고 원천과 나란히 읽어 내린 것이다. 방문·수집만으로 KEEP 을 만들지 않았고
Playwright 통과도 의미 검토로 계산하지 않았다. 각 화면에서 확인한 것:
핵심 질문 / 첫 화면 제공 정보 / 원천에 있으나 빠진 정보 / 차트·표·KPI 의 데이터 적합성 /
선택조건과 결과의 일치 / 불필요한 내부정보·중복설명.

판정값: KEEP · FIX_ANALYSIS · FIX_COPY · FIX_LAYOUT · DATA_LIMITATION · NO_DATA
"수정함" 은 이번 세션에서 고치고 해당 화면을 다시 수집해 확인한 것이다.

## 검토 결과 요약

| 항목 | 값 |
|---|---|
| 의미 검토한 화면 | 152 / 152 |
| 원천에 값이 없는 화면(NO_DATA) | 5 (C-020 · C-021 · C-023 · E-011 · E-013) |
| 선택조건이 있는 화면 | 94 — 전부 결과가 바뀜 |
| 화면에 남은 내부 코드·수집기록·작업메모 | 0 |
| 제공기관·자료기간을 말하지 않는 화면 | 0 |
| 런타임 오류가 난 화면 | 0 |

---

## 기준화면 (완성도 참조)

| 요소 | 판정 | 근거 |
|---|---|---|
| A-016 1차 에너지 소비구조 | KEEP | 단위(EJ)·관측기간(1965–2025, 61개 연도)·분모("여섯 에너지원 합계를 100%")·중복 경고("공급 총계는 구성항목과 중복되므로 면적 합계에 포함하지 않습니다")를 모두 화면에서 말한다. 2025년 총공급 4.699 EJ·석탄 2.569 EJ 가 원천값과 일치 |
| D-011 개도국 내 각국별 ODA | KEEP | 불변가격 기준(USD 2024)·기간(2020–2024)·제공기관(OECD CRS)·산정기준(대베트남 ODA 총지출액)을 명시 |
| B-005 가뭄 위험 | FIX_ANALYSIS → 수정함 | 31,688행을 두고 "관측값이 없습니다"라고 표시하고 있었다. 63개 성·시 분포로 교체 |

## 시나리오·성/시 계열 (원천이 개체 속성 열로 납품)

관측 시트 대신 개체 속성 열로 값이 오면서 관측 기반 렌더러가 아무것도 찾지 못했다.

| 요소 | 판정 | 확인한 내용 |
|---|---|---|
| B-003 평균 기온·강수 | FIX_ANALYSIS → 수정함 | 8,371행. 연강수량 median 1,637mm(1901) → 1,724mm(2025), 63개 성·시 분위 |
| B-004 CMIP6 기후전망 | FIX_ANALYSIS → 수정함 | 33,232행 · 5개 측정항목 · 6개 시나리오 |
| B-005 가뭄(SPEI12·CDD) | FIX_ANALYSIS → 수정함 | 31,688행 |
| B-006 폭염 | FIX_ANALYSIS → 수정함 | 32,608행 |
| B-007 홍수(RX1day 등) | FIX_ANALYSIS → 수정함 | 32,370행 |
| B-017 물 스트레스 | FIX_ANALYSIS → 수정함 | 2,032행. 성·시 분포로 표시하고, 유역 코드·대수층 코드는 표시항목에서 제외 |
| B-026 유향 · B-030 산림 이득 · B-037 토지피복 · B-039 수력잠재 · B-040 지열 · B-041 태양광 · B-042 풍력 | FIX_ANALYSIS → 수정함 | 성·시 1개 연도만 제공되는 계열이라 연도축 기준으로는 전부 탈락하고 있었다. 측정항목별로 판정하도록 바꿔 7개 화면이 63개 성·시 분포를 표시한다 |
| B-029 산림 유형별 면적 | FIX_ANALYSIS → 수정함 | 성·시 이탄지 분포와 전국 계열(맹그로브·습윤열대림 등)을 함께 표시. 전국 계열은 지표명·값·단위를 카드에 표시 |
| B-008 해수면 상승 | FIX_COPY → 수정함 | 카드 제목이 `1475_low_ssp126_q17_2020` 이었다. 관측소·시나리오·분위수·연도로 구성 |

## 공통 수정 (여러 화면에 걸침)

| 결함 | 영향 | 조치 |
|---|---|---|
| 원시 레코드 키가 카드 제목 (`VNM.1_1`, `1952-0008-VNM`) | 19개 화면 | 식별자 열의 값과 같은 이름은 이름이 아니므로, 요소별 사실 조합으로 대체 |
| 값이 이름 자리에 (`220`, `1.2`, `2004-11-29`) | A-013 · A-024 · A-029 | 숫자·날짜만인 이름이면 검토된 조합 제목을 사용 |
| 한 화면에 같은 제목의 카드가 여러 개 | 8개 화면 | 실제로 다른 값을 가진 항목(연도·라운드·구간 번호)을 제목에 덧붙임 |
| 선택 조건이 개체 목록에 적용되지 않음 | 9개 화면 | 개체가 가진 차원 열에도 선택을 적용 |
| 선택지가 하나뿐인 선택상자 | 32개 화면 + 지도 비교 2개 창 | 고를 것이 없으면 선택상자 대신 값을 표시 |
| 용어 풀이가 이미 쓰인 말을 괄호로 덧붙임 (`메탄(CH₄(메탄) 환산)`) | 지표명이 약어를 병기한 모든 화면 | 본문이 이미 말하고 있으면 덧붙이지 않음 |
| 같은 기관의 서로 다른 원문 링크가 모두 "World Bank 확인" | 링크가 여럿인 화면 | 링크마다 인용된 지표명을 붙임 |
| 연도가 천단위로 표시 (`2,014`) | C 계열 · E-008 | 연도는 수량이 아니므로 자리표를 넣지 않음 |
| 내부 코드·수집기록·작업파일명이 공개 문구에 노출 | 34개 화면 | `[DoD S-07 …]`, `raw: …md`, `[4차 정정]`, `속성 열 참조`, `1.2_entity`, `수집현황 v5.29` 제거 |

## 데이터 결함 (수치가 틀렸던 것)

| 요소 | 결함 | 조치 |
|---|---|---|
| B-031 | 분석대상 면적·수관 면적(2000)·수관 면적(2010) 세 항목이 **모두 같은 값**(성 경계 면적)이었다. 원천 열 이름이 `분석대상 면적(ha)` → `ha`, `수관 면적 2000(ha)` → `ha_2` 로 접히면서 세 항목이 첫 열에 붙었다 | 인쇄된 열 이름으로 결속. An Giang 352,425 / 38,545 / 31,815 ha 로 원천과 일치 |
| E-008 · E-014 · E-018 | 납품본이 열을 삽입해 V1 슬롯 이름이 한 칸씩 밀렸다. E-014 는 체결국이 `signedDate`, 서명일이 `scope`, 적용범위가 `status`, 현행 여부가 `sourceUrl` 로 발행되고 있었다 | 열 이동을 감지해 이름으로 재결속 |
| B-034 | `Mg CO2e/yr` 와 `Mg CO₂e/yr`, `지상부 탄소밀도` 와 `지상부 탄소 밀도` 가 서로 다른 항목으로 갈라져 같은 계열이 12개 항목으로 나뉘어 있었다 | 단위·명칭 표기를 하나로 정규화. 항목 12 → 7 |
| B-031 · B-032 · B-033 · B-034 | 제공기관이 "공개 자료에 기관명이 명시되지 않음" 이었다 | 워크북 자체 메타데이터의 기관명을 파생 관측에 전달 (Global Forest Watch 등) |
| C-016 | 성·시 639건을 발행하고도 화면에는 아무 수치도 없었다(연도축이 없어 추세가 성립하지 않음) | 연도축이 없으면 항목별 비교로 표시 |
| A-002 | 유의사항이 "공개된 CPIA 값은 2005~2015년까지" 라고 말하는데 화면은 1996–2024 WGI 를 그리고 있었다. 홈 카드도 "2005–2015년" | 납품된 계열에 맞는 유의사항으로 교체, 홈 카드는 실제 관측기간 사용 |
| 후보 빌드 | `public/data` 전체를 제외해 `world-countries.geojson` 이 후보에 들어가지 않았다. 지도가 매번 MapLibre 파싱 오류를 기록하고 있었다(CDP 감사는 콘솔을 읽지 않아 드러나지 않음) | vietnam 외 public/data 자산도 후보에 포함 |

## 기본 표시 항목 (첫 화면이 요소의 이름과 다르던 것)

| 요소 | 기본값 | 조치 |
|---|---|---|
| A-007 인구 | 도시인구 | 총인구 |
| A-012 온실가스 총배출량 | LULUCF 순플럭스(음수) | 총 GHG 배출량(LULUCF 제외) |
| A-027 · A-028 교통·수자원 인프라 | 명칭 결측률 등 레이어 메타데이터 | 분류별 피처 수. 레이어 메타데이터는 기본값에서 후순위로 |
| B-001 건기/우기 | 연 평년강수 1건 | 월 평년강수 12개월 |
| B-002 기후대 | 국가 우세 기후대 1건 | 기후대별 점유 면적 7개 |
| B-019 장기 인구 전망 | 인구 과거 실적(참고) | 인구 전 구간(SSP) |

## 자료 없음 (원천에 값이 없음 — 결함 아님)

| 요소 | 판정 | 화면이 말하는 것 |
|---|---|---|
| C-020 · C-021 · C-023 | NO_DATA | "아직 공개된 값이 없습니다 공식 자료를 확보한 뒤 제공합니다" |
| E-011 · E-013 | NO_DATA | "입력 항목만 있고 실제 값은 아직 없습니다" |

## 남은 자료 한계 (원천의 문제이며 화면은 사실대로 말함)

| 요소 | 내용 |
|---|---|
| A-026 건물 풋프린트 | 원천이 레이어 메타데이터(좌표계·속성 필드 수·신뢰도 하한)만 채웠고 피처 수·면적은 공란이다. 화면은 있는 것만 말한다 |
| B-013 CBAM | 4개 업종의 탄소지불액이 분모가 서로 달라 한 축에 놓지 않는다(`singleDenominatorDimensionKeys`). 한 번에 한 업종만 표시된다 |
| B-032 | 값(수관 피복률 10.94% 등)이 B-031 의 2000년 열과 67/68 행 일치하는데 원천 메타데이터는 기준연도를 2010으로 적는다. 원천 내부의 불일치이므로 값과 표기를 그대로 두고 여기 기록한다 |
| B-043 · B-044 | 세부 분류 값에 국가·연도·연료가 섞여 있다. 원천의 분류 열 자체가 그렇게 납품되었다 |
| D-025 · D-026 | 분야 값이 영문 계층 코드(`Energy/Electricity/…`)이거나 보증 유형 설명이다. 원천 표기를 그대로 둔다 |
| C-009 | 법령 1건이 속성마다 한 행이어서 타임라인에 같은 법령명이 여러 번 나온다. 원천 행 구조 |
